/**
 * Created by seet on 08.01.17.
 */
(function () {
    //Глобальная переменная
    var message = 'Привет';

    //Функция для вывода переменной
    function showMessage() {
        alert(message);
    }

    //Вывод сообщения
    showMessage();
})();

